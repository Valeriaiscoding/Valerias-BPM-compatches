REPLACE_OR_CREATE:law_colonial_exploitation = {
	group = lawgroup_colonization
	
	icon = "gfx/interface/icons/law_icons/colonial_exploitation.dds"
	
	progressiveness = -25

	can_enact = {
		trigger_if = {
			limit = { Ultra_D_is_active_trigger = yes }
		}
		has_technology_researched = civilizing_mission
	}

	is_visible = {
		OR = {
			AND = {
				com_law_colonial_exploitation_trigger = yes
				# Any vanilla conditions that have been added
				always = yes
			}
			com_law_colonial_exploitation_alternative_trigger = yes
		}
	}

	unlocking_technologies = {
		colonization
	}
	
	modifier = {
		building_group_bg_manufacturing_unincorporated_throughput_add = -0.25 # -0.1
		building_group_bg_plantations_unincorporated_throughput_add = 0.25 # 0.2
		building_group_bg_rubber_unincorporated_throughput_add = 0.25 # 0.2
		building_group_bg_logging_unincorporated_throughput_add = 0.25 # 0.2
		building_group_bg_mining_unincorporated_throughput_add = 0.25 # 0.2
		state_migration_pull_unincorporated_mult = -0.5
		state_unincorporated_starting_wages_mult = -0.25 # -0.1
		building_unincorporated_subsistence_output_mult = -0.10
		country_tension_decay_mult = -0.25
	}
	
	institution = institution_colonial_affairs
	
	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_colonization_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_will_do = {
		OR = {
			has_law_or_variant = law_type:law_colonial_resettlement
			navy_size >= 50
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_taming_the_north
			}
			add = 500
		}
	}

	ai_impose_chance = {
		value = 0
		
		if = {
			limit = {
				has_law = law_type:law_colonial_exploitation
				scope:target_country = {
					has_law_or_variant = law_type:law_no_colonial_affairs
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_colonial_offices }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}
