# Kinda too much

bpm_ultra_p_amendment_two_party_system_modifier = {
	allowed_laws = {
		law_presidential_republic
	}

	modifier = {
		country_legitimacy_ideological_incoherence_mult = -0.1
		country_legitimacy_govt_size_add = -1
	}

	possible = {
		trigger_if = {
			limit = { this = c:GBR }
			OR = {
				has_law = law_type:law_presidential_republic
				has_law = law_type:law_monarchy
			}
		}
		trigger_else = {
			has_law = law_type:law_presidential_republic
		}
		NOT = {
			has_law_or_variant = law_type:law_party_single
		}
	}
	can_repeal = { 
		trigger_if = {
			limit = { this = c:GBR }
			OR = {
				NOT = {
					has_law = law_type:law_presidential_republic
					has_law = law_type:law_monarchy
				}
				has_law_or_variant = law_type:law_party_single
			}
		}
		trigger_else = {
			OR = {
				NOT = {
					has_law = law_type:law_presidential_republic
				}
				has_law_or_variant = law_type:law_party_single
			}
		}
		
	}
	would_sponsor = {
		always = no
	}
	ai_will_revoke = { 
		always = no
	}
}