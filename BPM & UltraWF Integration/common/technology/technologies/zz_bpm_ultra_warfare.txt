INJECT:army_reserves = {
	modifier = { # moved to army model laws
		country_institution_conscription_max_investment_add = -1 #new
	}
}
INJECT:enlistment_offices = {
	modifier = { # moved to officer laws
		country_institution_conscription_max_investment_add = -1 #new
	}
}

INJECT:war_propaganda = {
	modifier = { # moved to officer laws
		country_institution_conscription_max_investment_add = -1
	}
}