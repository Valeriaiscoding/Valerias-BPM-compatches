TRY_REPLACE:dp_stts_install_dictator = { 
    war_goal = stts_install_dictator

    texture = "gfx/interface/icons/war_goals/stts_install_dictator.dds"

    possible = {
        NOT = { is_country_type = decentralized }
        aggressive_diplomatic_plays_permitted = yes
        NOT = {
            is_subject_of = scope:target_country
        }
        scope:target_country = {
            NOT = { is_country_type = decentralized }
            NOT = { is_subject_of = root }
        }
        scope:target_country = {
            OR = {
                is_subject_of = root
                is_subject = no
            }
        }
        trigger_if = {
            limit = {
                has_law = law_type:law_answers_to_legislature
            }
            var:bpm_headofgov ?= {
                bpm_leader_is_anarchist = no
                NOR = {
                    has_ideology = ideology:ideology_utopian
                    has_ideology = ideology:ideology_radical
                }
            }
        }
        trigger_else_if = {
            limit = {
                bpm_has_gov_regency = yes
            }
            var:bpm_regent ?= {
                bpm_leader_is_anarchist = no
                NOR = {
                    has_ideology = ideology:ideology_utopian
                    has_ideology = ideology:ideology_radical
                }
            }
        }
        trigger_else = {
            ruler = {
                bpm_leader_is_anarchist = no
                NOR = {
                    has_ideology = ideology:ideology_utopian
                    has_ideology = ideology:ideology_radical
                }
            }
        }
       
    }

    ai_acceptance_max = 90

    on_weekly_pulse = {}

    on_war_begins = {}

    on_war_end = {}
}